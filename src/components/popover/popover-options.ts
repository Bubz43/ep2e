export enum Placement {
  Top = 'top',
  Right = 'right',
  Bottom = 'bottom',
  Left = 'left',
}

export enum Origin {
  Outset = 'outset',
  Inset = 'inset',
  Pointer = 'pointer',
}

export enum OpenEvent {
  Click = 'click',
  Context = 'contextmenu',
  Hover = 'pointerenter',
}
